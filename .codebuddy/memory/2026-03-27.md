# 2026-03-27 工作日志

## 市场概览接入真实指数数据

**需求**: 将股票页面的市场概览从硬编码假数据改为接入后端 AKShare 真实指数行情。

**后端修改** (`backend/app/routers/stocks.py`):
- 新增 `GET /api/stocks/market/indices` 端点
- 调用 `MarketDataService._get_indices()` 获取实时指数数据（上证/深证/创业板/科创50/沪深300）
- 导入 `get_market_data_service` 工厂函数
- 端点放在 `/{stock_code}` 路径参数路由之前，避免路由冲突

**前端 API 修改** (`mobile/services/api.ts`):
- 新增 `getMarketIndices()` 方法，包含完整 TypeScript 类型定义

**前端页面修改** (`mobile/app/(tabs)/stocks.tsx`):
- 移除硬编码的3条假指数数据（上证/深证/恒生）
- 新增 `indices` 和 `indicesLoading` state
- 新增 `fetchIndices()` 函数，页面加载和下拉刷新时调用
- 支持显示最多5个指数卡片（上证/深证/创业板/科创50/沪深300）
- 支持 loading 状态和空数据（非交易时段）提示
- indexContainer 改为 `flexWrap: 'wrap'` 适配5个卡片布局

**待部署**: 需要推代码到 Gitee → 服务器拉取 → 重启 finance-daily 服务

## 部署及调试

- 首次部署后接口无响应（curl 返回 exit code 52 / 空响应），原因是 `MarketDataService._get_indices()` 内部的重试机制太重
- 解决方案：在路由端点中直接调用 `akshare.stock_zh_index_spot_em()`，绕过 Service 层的重试逻辑，用 `run_in_executor` + `asyncio.wait_for(timeout=30)` 异步执行
- 服务器代码路径：`/var/www/finance-daily`（不是 `/root/daily_finance`）
- 本地远程配置：`gitee` → `https://gitee.com/liyanli16/daily_finance.git`，`origin` → GitHub
- 最终 3 次 commit：`3be5d6f` → `aea7690` → `b9f4a92`
- ✅ 接口 `GET /api/stocks/market/indices` 正常返回5大指数实时数据（上证/深证/创业板/科创50/沪深300）

## UI/UX 修复与功能优化

### StockCard null crash 修复
- `StockCard.tsx` 中 `stock.change_percent !== undefined` 改为 `!= null`（覆盖 null 和 undefined）
- 同样修复 `stock.current_price` 的判断

### 股票页 FAB 按钮被 Tab Bar 遮挡
- `stocks.tsx` FAB 的 `bottom: 16` → `bottom: 80`

### 市场指数加载错误处理
- 新增 `indicesError` state，加载失败显示重试按钮

### API 超时与端口修复
- `api.ts` BASE_URL 从 `:8000` 改为 port 80（nginx 反向代理）
- 添加 AbortController 超时控制，默认15s，市场指数45s

### 播客页历史播客浮层
- 播放列表按钮改为打开页内底部弹出面板（非跳转到首页 Tab）
- 去掉黑色半透明遮罩，加强阴影替代
- 底部 padding 避免 Tab Bar 遮挡

### Tab Bar 不透明度
- `colors.ts` Tab Bar 背景 opacity 从 0.6 → 0.92

### 设置页
- 保留推送通知、内容偏好、自动下载等设置项（供未来后端集成使用）
- 新增服务器地址显示和使用说明弹窗

## PWA 部署准备

- Expo Web 构建成功：`npx expo export --platform web` → `/mobile/dist/`
- 创建 PWA manifest.json（名称"财经日报"、standalone、紫色主题、192/512 图标）
- index.html 添加 iOS PWA meta tags（apple-mobile-web-app-capable 等）
- 生成 app-icon-192.png 和 app-icon-512.png
- **EdgeOne Pages 部署失败**：报错 "finance has already been used"，全局命名空间占用，无法通过插件部署
- 改为手动上传到 EdgeOne Pages 控制台（项目名 `caijing-ribao`），但默认域名只提供3小时限时预览，不支持永久访问

## PWA 部署到云服务器（最终方案）✅

- 通过腾讯云 WebShell（OrcaTerm）登录服务器（SSH 微信扫码登录在终端超时失败）
- 服务器 `git pull` 从 Gitee 拉取最新代码（含 `mobile/dist/` 构建产物）
- 修改 nginx 配置 `/etc/nginx/sites-enabled/finance-daily`：
  - `location /` → `root /var/www/finance-daily/mobile/dist`，添加 `try_files $uri $uri/ /index.html` 支持 SPA 路由
  - `location /manifest.json` → `default_type application/manifest+json`
  - `/api/` 和 `/podcasts/` 保持不变
- ✅ **部署成功**：`http://82.156.59.2` 返回 HTTP 200，PWA meta 标签完整
- iPhone 安装方式：Safari 访问 → 分享 → 添加到主屏幕 → 全屏 App 体验
