# 2026-03-26 工作日志

## 完全移除微信公众号采集功能

**原因**: WeWe-RSS（微信读书登录失败）和 RSSHub（公众号路由已被官方删除）方案均不可用，用户决定放弃公众号数据源。

**删除的文件（4个）：**
- `backend/app/models/wechat.py`
- `backend/app/routers/wechat.py`
- `backend/app/services/wechat_service.py`
- `mobile/app/wechat-accounts.tsx`

**修改的文件（9个）：**
- `backend/app/main.py` — 移除 wechat 路由导入、启动初始化、路由注册
- `backend/app/services/__init__.py` — 移除 WechatSubscriptionService 导出
- `backend/app/models/__init__.py` — 移除 Wechat 模型导出
- `backend/app/models/database.py` — 移除 WechatAccountModel ORM 类
- `backend/app/services/news_collector.py` — 移除 `_collect_wechat_articles()` 调用和方法
- `backend/test_rss_sources.py` — 移除公众号 RSS 源
- `web/index.html` — 移除公众号订阅 HTML 和 JavaScript（约 300 行）
- `mobile/services/api.ts` — 移除公众号 API 方法（66 行）
- `mobile/app/(tabs)/settings.tsx` — 移除公众号设置入口和 router 导入

**总计**: 13 个文件，删除 2092 行代码。已本地 git commit（`f53de19`）。

**待完成**: ~~推送到 GitHub（需认证）~~ + ~~部署到服务器 82.156.59.2~~ + ~~清理服务器上的 RSSHub Docker 容器~~。

## 修复 web/index.html 残留公众号 UI

上一次提交 `f53de19` 中 `web/index.html` 遗漏了 80 行公众号相关的 HTML 和 JS 代码（订阅区域、推荐公众号、已订阅列表、loadWechatAccounts/loadWechatPresets 调用）。

**修复提交**: `8e1868a` — 彻底清除 web/index.html 中残留的公众号订阅 UI。

## 部署完成

- ✅ 代码推送到 Gitee（`gitee.com/liyanli16/daily_finance`）
- ✅ 服务器 origin 已改为 Gitee（之前是 GitHub，因 GitHub 认证问题）
- ✅ 服务器拉取最新代码 `8e1868a`
- ✅ `finance-daily` 服务已重启，状态 active (running)
- ✅ Docker 容器（rsshub、wewe-rss）已确认不存在，无需清理
- ⚠️ 服务器 origin 现在指向 Gitee（`https://gitee.com/liyanli16/daily_finance.git`），不再是 GitHub

## 优化播客天气格式和报告深度

### 1. 天气播报修复（podcast_generator.py）
- 重写 `get_beijing_weather()` 方法
- 新增 50+ 条英中天气映射字典，确保天气描述始终为中文（不再念英文/KMH）
- 新增 `_get_aqi()` 方法，调用 `api.waqi.info` 获取北京空气质量指数
- 输出格式统一为：「北京今天天气x，气温x摄氏度到x摄氏度，空气质量指数x」

### 2. 报告 Prompt 重写（ai_analyzer.py）
- 7个模块精简为5个：操作建议 → 市场全景 → 自选股作战图 → 事件催化 → 风控仪表盘
- 角色从"分析师"改为"私人投资顾问"，每段分析必须回答"所以我该怎么做"
- 禁止模糊表述（"值得关注""需要注意"），必须给出明确的买/卖/观望建议
- 自选股要求给出：信号+目标价+止损位+仓位建议
- 增加"反共识观点"要求

### 3. ReportSectionType 枚举更新（report.py）
- 7模块枚举改为5模块：OPERATION_GUIDE / MARKET_PANORAMA / WATCHLIST_BATTLE / EVENT_CATALYST / RISK_DASHBOARD

### 4. 播客脚本优化（podcast_generator.py）
- 字数目标从 3000-6000 改为 2500-3750（对应10-15分钟）
- 报告精华提取上限从 2000 字降为 1500 字
- 核心观点过渡语从"核心判断"改为"操作建议"
- 结束语从"划重点"改为"操作要点"
- 去掉"废话不多说"等套话

**已部署**: 代码已推送到 Gitee，服务器已拉取并重启。报告生成正常（8分钟播客），但天气获取失败（服务器上是旧代码时的问题，重启后应修复），AKShare 部分接口有 ConnectionError 重试。

## Mobile App 前端重构 — Luminous Lens 设计系统

**设计参考**: stitch 文件夹中 4 套设计稿（浅色/深色 × 播放器/历史列表），设计系统文档 `aura_glass_v2/DESIGN.md`。

**核心决策**:
- 支持浅色/深色模式切换（+ 跟随系统）
- 保留 4 个 Tab：首页/播客/自选股/设置
- 先做播客播放器 + 历史列表，其他后面再说
- 风格：玻璃拟态（Glassmorphism）、紫色主色调 `#7C4DFF`、Manrope 字体风格、无边框线
- 脱离 React Native Paper 组件库，改用原生 View/Text + 自定义样式

**新增文件**:
- `mobile/theme/colors.ts` — 完整浅色/深色色彩方案（Luminous Lens）
- `mobile/theme/ThemeContext.tsx` — 主题上下文 + AsyncStorage 持久化

**重写文件**:
- `mobile/app/_layout.tsx` — 用 ThemeProvider 替代 PaperProvider
- `mobile/app/(tabs)/_layout.tsx` — 半透明 Tab Bar
- `mobile/app/(tabs)/podcast.tsx` — 歌词同步滚动播放器 + 玻璃控制面板
- `mobile/app/(tabs)/index.tsx` — 玻璃拟态今日报告卡片 + 历史存档列表
- `mobile/app/(tabs)/settings.tsx` — 主题切换 UI + 玻璃卡片风格设置

**新增依赖**: expo-linear-gradient, expo-blur

**待完成**: 用户需要装 Expo Go 在手机上预览效果。
